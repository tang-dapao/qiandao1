# 全流程测试方案（签到 + 问题反馈 + 看广告轮转）

> 项目：D:\qiandao　设备：MuMu（emulator-5554，小米 12 机型）　日期：2026-09-09

## 一、测试目标

验证 `main.py` 全流程在真实设备上的正确性，覆盖本轮修复 + 之前验证过的链路：

1. **每日签到**：12 台逐台签到，验证签到浮层 → 成功/已签到 → 关闭
2. **问题反馈**：12 台逐台反馈，验证反馈页进入 → 返回
3. **看广告轮转**：多机 60s CD 轮转、广告关闭三层策略（uiautomator → 全屏 OCR ymax=400 → 固定坐标）、失败跳过（fail≥3 踢出）、死循环修复（P0）

## 二、测试范围与耗时（关键）

全量 12 台 × 10 次广告 ≈ **5 小时**，必须分阶段。建议三阶段：

| 阶段 | 范围 | 命令 | 预计耗时 |
|---|---|---|---|
| **1 冒烟** | 2 台，签到+反馈+每台 1 次广告 | `py -3.13 main.py --robots "古禹,裴旖" --ad-times 1` | ~10 分钟 |
| **2 全量签到反馈** | 12 台，签到+反馈+每台 1 次广告 | `py -3.13 main.py --ad-times 1` | ~50 分钟 |
| **3 全量广告（可选）** | 12 台 × 10 次广告 | `py -3.13 main.py --ad-only --ad-times 10` | ~5 小时挂机 |

## 三、前置条件

- [ ] MuMu 模拟器已开启（adb `emulator-5554` 在线）
- [ ] QQ 在前台，停在「联系人 → 机器人」列表页
- [ ] `config.yaml` 的 `udid = "emulator-5554"`（已配好）
- [ ] 编译通过（已确认）

## 四、测试步骤与验证点

### 阶段 1（冒烟，2 台）

**命令**：`py -3.13 main.py --robots "古禹,裴旖" --ad-times 1`

**验证点**（逐条核对日志）：

| # | 验证点 | 预期 |
|---|---|---|
| 1 | 入口探活 | 日志无"设备不在线"，正常开始 |
| 2 | 签到 | 每台日志 `== 每日签到 ==` + "签到流程完成"，按钮变已签到 |
| 3 | 反馈 | 每台日志 `== 问题反馈 ==`，正常进入并返回 |
| 4 | 广告关闭路径 | 日志显示「uiautomator 路径 / OCR 路径 / 固定坐标兜底」之一，且"广告已关闭" |
| 5 | 60s CD 轮转 | 两台间出现"全部在看广告 CD，等待"日志 |
| 6 | 退出干净 | 每台退回机器人列表，下一台能正常进入 |

### 阶段 2（全量签到反馈 + 每台 1 次广告）

**命令**：`py -3.13 main.py --ad-times 1`（12 台全量）

**验证点**：

- 12 台签到全部完成，无"未找到 每日签到 行"
- 12 台反馈全部完成
- 12 台广告各 1 次全部关闭，无"多次尝试后广告仍未关闭"
- 全程无死循环、无卡广告页、无 ERROR 级异常

### 阶段 3（全量广告，可选）

**命令**：`py -3.13 main.py --ad-only --ad-times 10`

**执行方式**：后台运行 + 定时检查日志（前台跑会超时）

## 五、通过 / 失败判定

- ✅ **通过**：所有机器人签到/反馈/广告成功，无死循环，无"广告仍未关闭"，无 ERROR 级异常
- ❌ **失败**：任一机器人连续 `fail≥3` 被踢出、广告卡住无法关闭、死循环、签到/反馈异常

## 六、风险与注意事项

1. **长测试必须后台运行**（bash 前台约 2 分钟超时），阶段 2/3 建议 `run_in_background` + 日志监控
2. 中途设备断开/卡住 → 按 MEMORY 恢复流程处理（snap 看页面 → 退回列表）
3. 看广告是真实广告内容，关闭按钮因广告类型走不同路径属**正常现象**，只要最终关闭成功即可
4. 阶段 3 的 5 小时建议**分多次跑**或用户本地挂机，不一次性挂满

## 七、需要你配合的事项

1. 保持 MuMu 开启、QQ 停在机器人列表页
2. 阶段 1 冒烟我先跑给你看；阶段 2/3 建议你本地跑或我后台跑 + 你看日志
3. 测试中若看到设备卡在异常页，随时告诉我

---

**待你确认**：
1. 是否按「阶段 1 冒烟 → 阶段 2 全量签到反馈」先跑，阶段 3（5 小时全量广告）暂缓？
2. 阶段 2 的每台广告次数用 1 次还是别的？
